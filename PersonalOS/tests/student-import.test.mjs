import test from 'node:test';import assert from 'node:assert/strict';import {mapStudentImport} from '../lib/student-import.mjs';
const input=()=>({Sources:[{source_id:'s',platform:'rednote',extra:'keep'}],Runs:[{run_id:'r'}],Posts:[{post_id:'xiaohongshu:3984644254597892346',source_id:'s',platform:'rednote',run_id:'r',caption:'Original\ntext',like_count:'0'}],Media:[{media_id:'m',post_id:'xiaohongshu:3984644254597892346',media_type:'image',drive_url:'https://drive.google.com/example',reverse_engineering_prompt:'Keep this prompt'}]});
test('student mapping retains large IDs, zero metrics, original text, analysis and extra fields',()=>{const m=mapStudentImport(input(),'workspace');assert.equal(m.posts[0].post_id,'xiaohongshu:3984644254597892346');assert.equal(m.posts[0].platform,'xiaohongshu');assert.equal(m.posts[0].like_count,'0');assert.equal(m.posts[0].metadata.inspiration_origin,'student-import');assert.equal(m.runs[0].metadata.inspiration_origin,'student-import');assert.equal(m.runs[0].metadata.cost_status,'unknown');assert.equal(m.sources[0].metadata.original_fields.extra,'keep');assert.equal(m.media[0].reverse_engineering_prompt,'Keep this prompt');});
test('missing relationships and numeric IDs rejected before writes',()=>{let d=input();d.Sources=[];assert.throws(()=>mapStudentImport(d,'w'));d=input();d.Posts[0].post_id=123;assert.throws(()=>mapStudentImport(d,'w'));d=input();d.Posts[0].like_count='unknown';assert.throws(()=>mapStudentImport(d,'w'));});
